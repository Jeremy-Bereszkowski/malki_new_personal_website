import {fullName, title} from "../global";

const FooterData = {
    left: {
        header: fullName,
        subHeader: title,
    },
    right: {
        email: "Email:",
        phone: "Mobile:",
    }
}

export default FooterData
