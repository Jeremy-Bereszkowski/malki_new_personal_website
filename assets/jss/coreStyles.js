const extraSmallFont = {
    fontSize: "calc(16px + (20 - 16) * ((100vw - 300px) / (3000 - 300)))"
}
const extraSmall2Font = {
    fontSize: "calc(14px + (18 - 14) * ((100vw - 300px) / (3000 - 300)))"
}
const smallFont = {
    fontSize: "calc(16px + (24 - 10) * ((100vw - 300px) / (3000 - 300)))"
}
const largeFont = {
    fontSize: "calc(36px + (56 - 36) * ((100vw - 300px) / (3000 - 300)))"
}

const sectionHeight = {
    minHeight: "55vh"
}

export {
    extraSmallFont,
    extraSmall2Font,
    smallFont,
    largeFont,

    sectionHeight,
}
